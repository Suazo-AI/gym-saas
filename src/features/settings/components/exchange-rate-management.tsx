"use client";
import { useActionState } from "react";
import { updateExchangeRateAction,type ExchangeRateState } from "../actions/exchange-rate.actions";
export function ExchangeRateManagement({current,canManage}:{current:{nioPerUsd:string;effectiveAt:string}|null;canManage:boolean}){const [state,action,pending]=useActionState(updateExchangeRateAction,{ok:false} as ExchangeRateState);return <section className="mt-6 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-xs font-black uppercase tracking-[.16em] text-brand-green">Tipo de cambio</p><h2 className="mt-2 text-xl font-black text-ink">Valor actual del dólar</h2><p className="mt-3 text-3xl font-black text-ink">C${current?.nioPerUsd??"36.600000"} <span className="text-sm text-gray">por US$1</span></p><p className="mt-2 text-sm text-gray">El valor inicial es C$36.60. Los cambios solo afectan transacciones nuevas; el historial no se recalcula.</p>{canManage?<form action={action} className="mt-4 flex flex-col gap-3 sm:flex-row sm:items-end"><label className="text-sm font-bold">Nueva tasa<input className="mt-2 min-h-11 rounded-lg border border-slate-300 px-3" inputMode="decimal" name="nioPerUsd" defaultValue={current?.nioPerUsd??"36.60"} required/></label><button className="min-h-11 rounded-lg bg-brand-green px-4 font-black text-white" disabled={pending}>{pending?"Guardando…":"Actualizar tasa"}</button></form>:null}{state.message?<p className={`mt-3 text-sm font-bold ${state.ok?"text-green-700":"text-red-700"}`}>{state.message}</p>:null}</section>}
