import{createElement}from"react";import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it}from"vitest";import{LocalizedNav,PreferencesControls}from"./preferences-controls";
describe("global preferences",()=>{it("offers compact language and theme buttons",()=>{const html=renderToStaticMarkup(createElement(PreferencesControls));expect(html).toContain("Cambiar idioma");expect(html).toContain("Cambiar apariencia");expect(html).toContain("ES");expect(html).toContain("Sistema");expect(html).not.toContain("<select");});it("renders authorized navigation",()=>{const html=renderToStaticMarkup(createElement(LocalizedNav,{screens:[{code:"payments",name:"Pagos",route:"/payments"}],currentPath:"/payments"}));expect(html).toContain("Pagos");expect(html).toContain('aria-current="page"');});});
